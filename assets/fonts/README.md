# Fonts

Drop a `.ttf` here named to match `config/config.xml` (`<font path="assets/fonts/hud.ttf" .../>`).

If no font file is found, the app falls back to raylib's built-in font automatically.
A good free pick: "Kenney Future", "Press Start 2P", or any clean sans/pixel TTF.
